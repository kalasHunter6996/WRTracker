# Build artifacts

The generated `.mtmod` is written here by `tools/build_mtmod.py`.

The SWF is expected at:

`build/kalas.wrtracker.WrTrackerView.swf`

It is packaged into the archive as:

`res/packages/kalas/wrtracker/kalas.wrtracker.WrTrackerView.swf`
